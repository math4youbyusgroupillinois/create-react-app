options:
  cors: true
actions:
  action: ./actions/action.js
  action1: ./actions/action1.js
tasks:
  task:
    schedule: cron(5 8 ? * 0)
    action: giphy/getGifById
    accounts: {}
    monitor:
      action: giphy/getGifById
      accounts: {}
      trackBy: id
      array: data.featured_tags
      input:
        gifId: 0
paths:
  /paths0:
    get:
      action: giphy/getGifById
      accounts: {}
tests:
  test:
    action: giphy/getGifById
    accounts: {}
    input:
      gifId: 0
